{
    "cookies": {
        "description": "小黑盒 Cookie（可选）",
        "type": "string",
        "hint": "登录小黑盒后获取的 Cookie 字符串。也可以不填写，直接在聊天中使用「扫码登录」指令完成登录。详见 README。",
        "default": ""
    },
    "login_timeout": {
        "description": "扫码登录等待超时（秒）",
        "type": "int",
        "hint": "发起扫码登录后，等待用户扫码确认的超时时间（秒）。超时后需重新发起。",
        "default": 120
    },
    "wait_timeout": {
        "description": "页面加载超时时间（毫秒）",
        "type": "int",
        "hint": "页面加载的超时时间，单位毫秒。",
        "default": 60000
    },
    "render_delay": {
        "description": "额外渲染等待时间（毫秒）",
        "type": "int",
        "hint": "在内容框架出现后，额外等待渲染的时间（毫秒），以确保图片等动态内容加载完成。",
        "default": 5000
    },
    "device_scale_factor": {
        "description": "截图清晰度（设备缩放因子）",
        "type": "float",
        "hint": "设置截图的设备缩放因子。设置为 2 可让文字更加清晰。取值范围 1~3。",
        "default": 2
    },
    "image_quality": {
        "description": "图片质量（JPEG）",
        "type": "int",
        "hint": "JPEG 格式的图片质量，取值 1~100。",
        "default": 95
    },
    "show_game_title": {
        "description": "截图前显示游戏名",
        "type": "bool",
        "hint": "是否在截图前以文本形式显示游戏名称。",
        "default": true
    },
    "show_online_count": {
        "description": "截图前显示当前在线人数",
        "type": "bool",
        "hint": "是否在截图前以文本形式显示当前在线人数。",
        "default": true
    },
   "enable_link_preview": {
       "description": "自动解析小黑盒链接",
       "type": "bool",
       "hint": "启用后，当用户发送小黑盒链接时，插件会自动解析该链接。默认使用截图回复；开启「文字模式」后改为文字模式。",
       "default": true
   },
   "link_text_parse": {
       "description": "文字模式",
       "type": "bool",
       "hint": "启用后，自动解析小黑盒链接时不再截图，而是提取正文文字与图片，并按原文顺序放入文字模式中。",
       "default": false
   },
   "debug": {
        "description": "调试模式",
        "type": "bool",
        "hint": "启用后，将在控制台输出详细的调试日志。",
        "default": false
    }
}
